[build]
  command = "vite build"
  publish = "dist/client"
[build.environment]
  NODE_VERSION = "24"
[functions]
  maxDuration = 26
  included_files = ["node_modules/.pnpm/**"]
[dev]
  command = "pnpm run dev"
  targetPort = 3000
  port = 8888
